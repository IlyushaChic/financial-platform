apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: financial-platform
spec:
  selector:
    app: notification-service
  ports:
  - port: 8081
    targetPort: 8081
    name: http
  type: ClusterIP